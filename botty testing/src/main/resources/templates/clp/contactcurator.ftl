{
  <#if request.curatorid??>"CuratorID": "${request.curatorid}"</#if>
  <#if request.ideadescription??>,"IdeaDescription": "${request.ideadescription}"</#if>
  <#if request.ideatext??>,"IdeaText": "${request.ideatext}"</#if>
  <#if request.boardid??>,"boardId": "${request.boardid}"</#if>
  }